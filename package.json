{
  "name": "pos-admin-lte",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./docs",
    "build": "parcel build index.html 404.html --out-dir ./docs --public-url /pos-admin-lte/",
    "dev": "parcel serve manage-customers.html --out-dir ./docs",
    "start": "npm run build && npm run dev"
  },
  "devDependencies": {
    "@types/big.js": "^6.1.2",
    "@types/jquery": "^3.5.6",
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "admin-lte": "^3.1.0",
    "big.js": "^6.1.1",
    "jquery": "^3.6.0"
  }
}
